import { IntroSection } from "@/entities/main-page/intro";
import { Layout } from "@/shared/ui/layout/layout";

export const MainPageLayout = () => {
  return (
    <Layout>
      <IntroSection />
    </Layout>
  );
};
